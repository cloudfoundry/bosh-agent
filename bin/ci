#!/usr/bin/env bash
set -eu -o pipefail

ROOT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

"${ROOT_DIR}/bin/lint" # TODO: this should be called in parallel via Concourse

exec "${ROOT_DIR}/bin/test-unit"
