#!/usr/bin/env bash
set -eu -o pipefail

ROOT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

base_gopath=$( cd ${ROOT_DIR}/../../../.. && pwd )

export GOPATH="${base_gopath}"
export GOBIN="${base_gopath}/gobin"
export PATH=${PATH}:${GOBIN}:"${base_gopath}/bin"

exec $@
