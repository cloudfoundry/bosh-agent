#!/bin/bash 

echo "WARNING!!! Cross-compilation from Mac OS will result in a broken binary. The source looks up the user home dir, which requires native libraries (Cgo)."

set -e

export GOARCH=amd64
export GOOS=linux
export GOTOOLDIR=$(go env GOROOT)/pkg/linux_amd64

$(dirname $0)/build
